import React from 'react';
import Layout from '@theme/Layout';

export default function CiCd() {
    return (
        <Layout title="Build &amp; Test During CI/CD" description="Spin up full-stack environments as part of your CI/CD pipeline">
            <section className="hero-section">
                <div className="hero-inner">
                    <span className="hero-badge">⚙️ Solution</span>
                    <h1 className="hero-title">Build & Test During CI/CD</h1>
                    <p className="hero-subtitle">
                        Spin up full-stack environments as part of your CI/CD pipeline
                    </p>
                </div>
            </section>
        </Layout>
    );
}
